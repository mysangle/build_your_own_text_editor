모든 C 프로그램은 main() 함수에서 시작한다. main() 함수가 끝나면 프로그램이 끝난다. main() 함수가 리턴하는 값이 0이면 성공을 의미한다.

파일의 컴파일은 아래와 같이 한다.

> cc kilo.c -o kilo

kilo.c를 컴파일하여 kilo라는 실행 파일을 만든다.
현재는 kilo를 실행하면 아무것도 프린트하지 않는다.
main()이 리턴하는 값을 알고 싶으면 아래의 명령을 입력한다.

> echo $?

