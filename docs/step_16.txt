read()가 정해진 시간동안 입력이 없으면 리턴하게 한다.

VMIN은 read()가 리턴하기전 input byte의 최소수를 의미한다. 0으로 주었으므로 read()는 input이 발생하면 바로 리턴한다.
VTIME은 read()가 리턴하기 전에 기다리는 최대시간을 의미한다. 10분의 1초가 기준이다. 1로 주었으므로 read()는 100 milliseconds 동안 입력이 없으면 0을 리턴한다.

